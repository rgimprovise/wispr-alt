# Production backend URL. Set this to your Fly.io (or other) deploy URL
# before running `bun run tauri build`. Vite picks it up automatically when
# NODE_ENV=production (which tauri build sets).
VITE_BACKEND_URL=https://wispr-alt.fly.dev
